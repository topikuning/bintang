"""AI features per use-case. Setiap modul = 1 fitur (kategori,
po-cover, justify, dll).

Convention: setiap modul export 1 fungsi `run(...)` yg load context dari
DB, panggil services/ai.chat(), return result terstruktur.
"""
