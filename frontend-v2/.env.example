# Base URL ke backend API. Default: pakai vite proxy.
# Untuk dev langsung ke backend: http://localhost:8000/api/v1
# Untuk prod (Railway): https://backend.up.railway.app/api/v1
VITE_API_BASE_URL=/api/v1

# Hanya dipakai oleh vite.config.ts utk dev proxy.
VITE_BACKEND_URL=http://localhost:8000
