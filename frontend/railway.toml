# Konfigurasi Railway untuk service frontend.
# Root Directory service di-set ke `frontend`.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
